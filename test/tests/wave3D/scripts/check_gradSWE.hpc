#PBS -N TestInverse3D
#PBS -l select=9:ncpus=48:mpiprocs=48
#PBS -l place=scatter:excl
#PBS -l walltime=1:00:00
#PBS -j oe
#PBS -P ne_ldrd
cd $PBS_O_WORKDIR

rm -r SWEmeas
cp -r SWE SWEmeas
cp inputs/GrGaussian.i inputs/GrMesh.i

../../../isopod-opt -i inputs/GrMesh.i grid_size=1
mv GrMesh1.e inputs/GrMesh.e

mpirun ../../../isopod-opt -i check_gradSWE.i

rm inputs/GrMesh.i
rm -r SWEmeas
